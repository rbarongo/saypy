# Frontend (React)

Install & run (Node.js required):

```
cd frontend
npm install
npm run dev
```

This starts the Vite dev server (default port 5173). The demo app posts uploads to `http://localhost:8000/upload`.
